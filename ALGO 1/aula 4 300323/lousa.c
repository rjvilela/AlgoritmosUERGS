/*
if - se 
main()
int n=10;
if(N>9)
if (n>10)
....

if (...)
{ (opcional)

} (opcional)
Operadores Lógicos
== igual
> maior
< menor
!= diferente
>= maior igual
<= menor igual

else = senão
---------------------------------------------------------------------------------------------------

agora estamos conversando sobre o programa para autoescola

idade sera uma VARIAVEL

a professora fez uma prtica de uso do if/else, sem os colchetes(bloco). Ela nos mostrou como
depois do else, se colocarmos mais de um comando sem ter aberto bloco, o printf (exemplo dado)
nao imprimiu corretamente.

ERRO: if (idade>= 18);
        printf("\n pode tirar carteira");
      else
        printf("\n nao pode tirar carteira");
    
  printf ("\n continuacao do programa");

---------------------------------------------------

outro exempli que ela deu foi o seguinte:
primeiro construiu um codigo com dois ifs = o primeiro com a condicao de idade >= 18
depois criou outro if dentro do primeiro if para condicao de idade <=69 anos...
e depois ela mostrou como que usar o comando relacional logico && para diminuir o numero de linhas do programa.

----------------------------------------------------------------------------------------------------
usamos muito o if para fazermos consistencia de dados (evitar que o usuario digite errado)



*/