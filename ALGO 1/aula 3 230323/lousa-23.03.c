/*operações:
    - Atribuição
   int main ()
    int num
    num = 5;

    * soma
        num = num + 2;

    * Subtração
        -
    * multiplicação
        *
    * divisão
        /
        ... 
        num = num/3;
        ...
        result = num/3;
    

    *Resto
        %
        num = num%2;

TIPOS DE VARIAVEIS:
    -char, int, float,

    *CAST : modificar o tipo do dado em tempo de execução
    .
    .
    .
    .
    acrescento entre parenteses outro tipo que desejo que seja modificado antes da operação
    ex: (float)num1/(float)num2;


como diminuir o uso da memoria ?

int num1, num2, result; ao inves de int num1, num2, soma, sub, mult, div, resto;
troquei 4 variaveis por 1 (result)

tambem posso remover o result tambem se eu quiser
    ex: printf ("%d",5); ?(o que vai acontecer?)
        printf("%d", num1+num2);

#include <stdio.h>
#include <math.h>

int main()
{
int num1, num2;
printf ("informe o primeiro numero: ");
scanf ("%d", &num1);
printf ("informe o segundo numero: ");
scanf ("%d", &num2);


printf (......)
}



    .
    .
    .
----------------------------------------------------------------------------------------------------

    
    */